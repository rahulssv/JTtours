apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: jttours
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend-container
          image: rahulssvibm/backend:latest
          # image: rahul1181/backend-image:latest
          envFrom:
            - configMapRef:
                name: backend-configmap
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: mysql-persistent-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-persistent-storage
          persistentVolumeClaim:
            claimName: mysql-pv-claim